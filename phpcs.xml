<?xml version="1.0"?>
<ruleset name="Atalaia Project Standard">
    <description>The PSR12 coding standard with legacy exceptions.</description>
    <arg name="colors"/>
    <arg value="p"/>

    <file>app</file>
    <file>tests</file>

    <rule ref="PSR12">
        <!-- Ignora obrigatoriedade de camelCase para métodos de teste (comum em testes) -->
        <exclude name="PSR1.Methods.CamelCapsMethodName.NotCamelCaps"/>
    </rule>

    <!-- Transforma erro de linha longa em apenas aviso -->
    <rule ref="Generic.Files.LineLength">
        <properties>
            <property name="lineLimit" value="120"/>
            <property name="absoluteLineLimit" value="0"/>
        </properties>
    </rule>

    <exclude-pattern>vendor/*</exclude-pattern>
    <exclude-pattern>storage/*</exclude-pattern>
    <exclude-pattern>bootstrap/cache/*</exclude-pattern>
</ruleset>